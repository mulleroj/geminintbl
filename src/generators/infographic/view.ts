import { colorModes, layoutPresets, stylePresets } from './presets';
import { buildInfographicPrompt, defaultInfographicState, type InfographicState } from './prompt-builder';

const esc = (value: unknown): string => String(value ?? '').replaceAll('&', '&amp;').replaceAll('<', '&lt;').replaceAll('>', '&gt;').replaceAll('"', '&quot;').replaceAll("'", '&#039;');
const options = (items: readonly (readonly [string, string])[], selected: string) => items.map(([value, label]) => `<option value="${esc(value)}"${value === selected ? ' selected' : ''}>${esc(label)}</option>`).join('');

export function renderInfographicGenerator(): string {
  return `<section class="generator-page wrap" data-generator="infographic"><div class="generator-header"><p class="eyebrow">Interní generátor</p><h1>Generátor promptu pro infografiku</h1><p class="lead">Z ověřených faktů z Gemini Notebook vytvořte brief a potom prompt pro vizuální workflow. Nástroj negeneruje hotovou grafiku.</p><div class="generator-notice"><strong>Nejdřív ověřte obsah.</strong><span>Poměr stran, layout, barevnost a typografie ovlivňují prompt, ne pravdivost zdrojů.</span></div></div><div class="generator-layout"><form class="generator-form" id="infographic-form"><fieldset><legend>1. Obsah a publikum</legend><div class="generator-field-grid"><label>Téma / hlavní předmět<input name="subject" required placeholder="Např. Oběh vody v přírodě" /></label><label>Cílové publikum<select name="audience" required><option value="">Vyberte…</option><option>Začátečníci / škola</option><option>Žáci 2. stupně</option><option>Profesionálové</option><option>Obecné publikum</option><option>Experti / technické</option></select></label><label>Typ struktury<select name="structure"><option>Úvod → hlavní body → závěr</option><option>Časová osa</option><option>Příčina → proces → dopad</option><option>Porovnání</option><option>Mapa pojmů</option></select></label><label>Další informace<textarea name="extra" rows="3" placeholder="Omezení, klíčová fakta, zdroje…"></textarea></label></div></fieldset><fieldset><legend>2. Vizuální nastavení</legend><div class="generator-field-grid"><label>Poměr stran<select name="ratio"><option>16:9</option><option>4:3</option><option>1:1</option><option>9:16</option><option>A4 portrait</option></select></label><fieldset class="segmented-field"><legend>Úroveň detailu</legend><div class="segmented-control" role="radiogroup" aria-label="Úroveň detailu"><label><input type="radio" name="detail" value="Minimalistická" /><span>Minimalistická</span></label><label><input type="radio" name="detail" value="Standardní" checked /><span>Standardní</span></label><label><input type="radio" name="detail" value="Detailní" /><span>Detailní</span></label></div></fieldset><label>Vizuální styl<select name="style">${options(stylePresets, defaultInfographicState.style)}</select></label><label>Layout preset<select name="layoutPreset">${options(layoutPresets, defaultInfographicState.layoutPreset)}</select></label><label>Barevný režim / paleta<select name="colorMode">${options(colorModes, defaultInfographicState.colorMode)}</select></label></div></fieldset><fieldset><legend>3. Kontrola kvality</legend><div class="generator-checks"><label><input type="checkbox" name="fixTypography" checked /> Hlídání typografie</label><label><input type="checkbox" name="spellcheck" checked /> Kontrola pravopisu</label></div></fieldset><div class="generator-actions"><button class="button button-primary" type="submit">Vygenerovat prompt</button><button class="button button-secondary" type="button" data-generator-copy>Kopírovat prompt</button><button class="button button-quiet" type="button" data-generator-reset>Reset</button></div><p class="generator-feedback" data-generator-feedback role="status" aria-live="polite"></p></form><aside class="generator-output"><div class="generator-output-head"><div><p class="eyebrow">Výstup</p><h2>Prompt pro infografiku</h2></div><span class="generator-badge">${stylePresets.length} stylů · ${layoutPresets.length} layoutů</span></div><label class="visually-hidden" for="infographic-output">Vygenerovaný prompt</label><textarea id="infographic-output" data-generator-output readonly aria-live="polite"></textarea><p class="generator-output-note">Konečný vizuál vytvořte až po kontrole zdrojů, čísel a čitelnosti.</p></aside></div></section>`;
}

function readState(form: HTMLFormElement): InfographicState { const value = (name: string) => (form.elements.namedItem(name) as HTMLInputElement | HTMLSelectElement | HTMLTextAreaElement)?.value ?? ''; const checked = (name: string) => Boolean((form.elements.namedItem(name) as HTMLInputElement)?.checked); return { subject: value('subject'), audience: value('audience'), structure: value('structure'), extra: value('extra'), ratio: value('ratio'), detail: value('detail') as InfographicState['detail'], style: value('style'), layoutPreset: value('layoutPreset'), colorMode: value('colorMode'), fixTypography: checked('fixTypography'), spellcheck: checked('spellcheck') }; }
async function copy(value: string): Promise<void> { try { await navigator.clipboard.writeText(value); } catch { const area = document.createElement('textarea'); area.value = value; document.body.append(area); area.select(); document.execCommand('copy'); area.remove(); } }

export function mountInfographicGenerator(root: HTMLElement): void { const form = root.querySelector<HTMLFormElement>('#infographic-form'); const output = root.querySelector<HTMLTextAreaElement>('[data-generator-output]'); const feedback = root.querySelector<HTMLElement>('[data-generator-feedback]'); if (!form || !output) return; const announce = (message: string) => { if (feedback) feedback.textContent = message; }; const generate = () => { output.value = buildInfographicPrompt(readState(form)); }; form.addEventListener('input', generate); form.addEventListener('submit', (event) => { event.preventDefault(); generate(); announce('Prompt aktualizován ✓'); }); root.querySelector('[data-generator-copy]')?.addEventListener('click', async () => { await copy(output.value); announce('Zkopírováno ✓'); }); root.querySelector('[data-generator-reset]')?.addEventListener('click', () => { form.reset(); generate(); announce('Nastavení obnoveno.'); }); generate(); }
